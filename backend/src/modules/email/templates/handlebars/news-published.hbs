<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>{{newsTitle}} - {{storeName}}</title>
  <style>
    /* Reset styles */
    body, table, td, a {
      -webkit-text-size-adjust: 100%;
      -ms-text-size-adjust: 100%;
    }
    table, td {
      mso-table-lspace: 0pt;
      mso-table-rspace: 0pt;
    }
    img {
      -ms-interpolation-mode: bicubic;
      border: 0;
      height: auto;
      line-height: 100%;
      outline: none;
      text-decoration: none;
    }

    /* Body styles */
    body {
      margin: 0;
      padding: 0;
      width: 100% !important;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background-color: #f4f7fa;
    }

    /* Container styles */
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
    }

    /* Header styles */
    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      padding: 40px 30px;
      text-align: center;
    }

    .header h1 {
      color: #ffffff;
      font-size: 28px;
      font-weight: 700;
      margin: 0 0 10px 0;
      line-height: 1.3;
    }

    .store-name {
      color: rgba(255, 255, 255, 0.9);
      font-size: 14px;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin: 0;
    }

    /* Badge styles */
    .badge {
      display: inline-block;
      background-color: rgba(255, 255, 255, 0.2);
      color: #ffffff;
      padding: 6px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
      margin-top: 15px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    /* Content styles */
    .content {
      padding: 40px 30px;
    }

    .greeting {
      color: #2d3748;
      font-size: 16px;
      margin: 0 0 20px 0;
    }

    .news-meta {
      display: flex;
      align-items: center;
      gap: 15px;
      margin: 0 0 25px 0;
      padding-bottom: 20px;
      border-bottom: 2px solid #e2e8f0;
    }

    .author {
      color: #667eea;
      font-size: 14px;
      font-weight: 600;
    }

    .date {
      color: #718096;
      font-size: 14px;
    }

    .separator {
      color: #cbd5e0;
      font-weight: 300;
    }

    /* Cover image styles */
    .cover-image {
      width: 100%;
      max-width: 540px;
      height: auto;
      border-radius: 12px;
      margin: 0 0 30px 0;
      display: block;
    }

    /* Text styles */
    .news-title {
      color: #1a202c;
      font-size: 24px;
      font-weight: 700;
      line-height: 1.4;
      margin: 0 0 20px 0;
    }

    .news-excerpt {
      color: #4a5568;
      font-size: 16px;
      line-height: 1.6;
      margin: 0 0 30px 0;
    }

    /* Button styles */
    .button-container {
      text-align: center;
      margin: 35px 0;
    }

    .button {
      display: inline-block;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: #ffffff !important;
      text-decoration: none;
      padding: 16px 40px;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
      transition: all 0.3s ease;
    }

    .button:hover {
      box-shadow: 0 6px 20px rgba(102, 126, 234, 0.5);
      transform: translateY(-2px);
    }

    /* Divider */
    .divider {
      height: 1px;
      background-color: #e2e8f0;
      margin: 40px 0;
    }

    /* Footer styles */
    .footer {
      background-color: #f7fafc;
      padding: 30px;
      text-align: center;
      border-top: 1px solid #e2e8f0;
    }

    .footer-text {
      color: #718096;
      font-size: 14px;
      line-height: 1.6;
      margin: 0 0 15px 0;
    }

    .footer-links {
      margin: 20px 0 0 0;
    }

    .footer-link {
      color: #667eea;
      text-decoration: none;
      font-size: 13px;
      margin: 0 10px;
    }

    .unsubscribe-link {
      color: #a0aec0;
      text-decoration: underline;
      font-size: 12px;
      margin-top: 15px;
      display: inline-block;
    }

    /* Social proof */
    .social-proof {
      background-color: #f7fafc;
      border-left: 4px solid #667eea;
      padding: 20px;
      margin: 30px 0;
      border-radius: 4px;
    }

    .social-proof-text {
      color: #4a5568;
      font-size: 14px;
      line-height: 1.6;
      margin: 0;
      font-style: italic;
    }

    /* Responsive styles */
    @media only screen and (max-width: 600px) {
      .email-container {
        width: 100% !important;
      }

      .header {
        padding: 30px 20px !important;
      }

      .header h1 {
        font-size: 24px !important;
      }

      .content {
        padding: 30px 20px !important;
      }

      .news-title {
        font-size: 20px !important;
      }

      .news-excerpt {
        font-size: 15px !important;
      }

      .button {
        padding: 14px 30px !important;
        font-size: 15px !important;
      }

      .footer {
        padding: 20px !important;
      }

      .footer-link {
        display: block;
        margin: 10px 0 !important;
      }
    }
  </style>
</head>
<body>
<div style="background-color: #f4f7fa; padding: 20px 0;">
  <table role="presentation" class="email-container" cellspacing="0" cellpadding="0" border="0" align="center" width="100%">
    <!-- Header -->
    <tr>
      <td class="header">
        <p class="store-name">{{storeName}}</p>
        <h1>Latest News</h1>
        {{#if hasCategory}}
          <span class="badge">{{category}}</span>
        {{/if}}
      </td>
    </tr>

    <!-- Main Content -->
    <tr>
      <td class="content">
        <!-- Greeting -->
        <p class="greeting">Hi {{userName}},</p>

        <!-- News Metadata -->
        <div class="news-meta">
          <span class="author">By {{authorName}}</span>
          <span class="separator">•</span>
          <span class="date">{{publishedAt}}</span>
        </div>

        <!-- Cover Image (if available) -->
        {{#if hasCoverImage}}
          <img src="{{coverImageUrl}}" alt="{{newsTitle}}" class="cover-image">
        {{/if}}

        <!-- News Title -->
        <h2 class="news-title">{{newsTitle}}</h2>

        <!-- News Excerpt -->
        <p class="news-excerpt">{{newsExcerpt}}</p>

        <!-- Call to Action Button -->
        <div class="button-container">
          <a href="{{newsUrl}}" class="button">Read Full Article</a>
        </div>

        <!-- Social Proof / Info Box -->
        <div class="social-proof">
          <p class="social-proof-text">
            💡 Stay updated with the latest news, product launches, and exclusive offers from {{storeName}}.
          </p>
        </div>

        <div class="divider"></div>

        <!-- Additional Info -->
        <p style="color: #718096; font-size: 14px; line-height: 1.6; margin: 0;">
          Thanks for being a valued member of our community! We're excited to share this update with you.
        </p>
      </td>
    </tr>

    <!-- Footer -->
    <tr>
      <td class="footer">
        <p class="footer-text">
          You're receiving this email because you're subscribed to news updates from <strong>{{storeName}}</strong>.
        </p>

        <div class="footer-links">
          <a href="{{newsUrl}}" class="footer-link">View Online</a>
          <span style="color: #cbd5e0;">|</span>
          <a href="{{newsUrl}}" class="footer-link">Visit Store</a>
          <span style="color: #cbd5e0;">|</span>
          <a href="mailto:support@{{storeName}}.com" class="footer-link">Contact Us</a>
        </div>

        <a href="{{unsubscribeUrl}}" class="unsubscribe-link">Unsubscribe from news updates</a>

        <p style="color: #a0aec0; font-size: 12px; margin: 20px 0 0 0;">
          © {{#year}}{{/year}} {{storeName}}. All rights reserved.
        </p>
      </td>
    </tr>
  </table>
</div>
</body>
</html>
