apiVersion: v1
kind: Secret
metadata:
  name: predictor-secret
  namespace: predictor
type: Opaque
stringData:
  # Auth token for predictor API
  PREDICTOR_AUTH_TOKEN: "change-me-in-production"

  # Database credentials
  PGUSER: "predictor_user"
  PGPASSWORD: "change-me-in-production"

  # Optional: model storage credentials
  # AWS_ACCESS_KEY_ID: ""
  # AWS_SECRET_ACCESS_KEY: ""
